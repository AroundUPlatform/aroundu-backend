apiVersion: v1
kind: Service
metadata:
  name: ranking-engine-service
  namespace: aroundu
  labels:
    app.kubernetes.io/name: ranking-engine
    app.kubernetes.io/part-of: aroundu
    app.kubernetes.io/component: ranking
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ranking-engine
  ports:
    - port: 50052
      targetPort: 50052
      protocol: TCP
      name: grpc
